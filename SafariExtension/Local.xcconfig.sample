// Local build settings for YTDL Bridge.
// Copy this file to Local.xcconfig and fill in your own values.
// Local.xcconfig is gitignored so your personal values stay off the repo.

// Your 10-character Apple Developer Team ID.
// Find it in Xcode > Settings > Accounts > <your Apple ID> > "Manage Certificates..." (top-right).
DEVELOPMENT_TEAM = XXXXXXXXXX

// OPTIONAL. Absolute path to the ytdl binary, used as the first autodetection
// candidate. Leave it empty for release builds: the app finds ytdl on its own
// (~/.local/bin, /opt/homebrew/bin, /usr/local/bin, then the login shell's PATH),
// and users can override the path in the app's own settings.
// Only set this if you want a specific binary preferred during development.
// Example: /Users/you/Documents/Development/ytdl/.venv/bin/ytdl
YTDL_BIN_PATH =
